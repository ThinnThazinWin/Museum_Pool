import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function MMap({navigation}) {
  return (
    <View>
      <Text>MAp</Text>
    </View>
  )
}

const styles = StyleSheet.create({})